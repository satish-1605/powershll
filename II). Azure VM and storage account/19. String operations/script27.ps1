$servername ="dbserver123456897"

# "dbserver123456897" is a string based object
$servername.Length
#there are so amny methods based on string based object like contain, clone , compare to and many more

$servername.Contains("dba")
$servername.EndsWith("dba")
$servername.IndexOf('7')